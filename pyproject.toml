[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "elyos-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx==0.28.1",
    "openai==2.43.0",
    "python-dotenv==1.2.1",
]

[tool.setuptools]
py-modules = ["cli"]
